import { Request, Response } from "express";

export const addTweetController = async (req: Request, res: Response): Promise<any> => {
  try {
    /* const safeData = .safeParse(req.body);

    if (safeData.error) {
      return res.json({ error: safeData.error.flatten().fieldErrors });
    } */


  } catch (error) {
  }
};
